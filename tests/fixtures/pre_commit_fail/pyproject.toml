[project]
name = "pre-commit-fail-fixture"
version = "0.0.0"
requires-python = ">=3.11"

[tool.forge]
paths = ["src"]
crap_threshold = 6

[tool.forge.gates]
crap = true
mutation = false
gherkin = false
dry = false
